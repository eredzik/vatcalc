import { Button } from "semantic-ui-react";
import { Layout } from "../../components/Layout";

export function Edit() {
  return (
    <Layout>
      {" "}
      <Button icon="trash" content="Usuń kontrahenta" onClick={() => {}} />
    </Layout>
  );
}
